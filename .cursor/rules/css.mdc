---
description: css编码规范
globs: **/*.scss, **/*.sass, **/*.css
alwaysApply: false
---

# CSS 样式规范

## 1. 基础规范

### 1.1 代码风格

- **分号**: 每个CSS规则后必须加分号
- **引号**: 属性值使用双引号，如`content: "text"`
- **空格**: 选择器与`{`之间使用一个空格，属性名与`:`之间无空格，`:`与属性值之间使用一个空格

### 1.2 命名规范

- **语义化**: 使用有意义的类名，避免使用缩写，保持命名风格一致性
- **前缀**: 组件样式使用组件名前缀，如`.button`、`.modal`

### 1.3 选择器规范

- **优先级**: 避免使用`!important`，除非绝对必要
- **嵌套深度**: CSS嵌套不超过3层
- **选择器长度**: 单个选择器不超过3个层级
- **性能**: 非必要不使用通配符选择器`*`

## 2. 布局规范

### 2.1 盒模型

- **box-sizing**: 统一使用`border-box`
- **margin**: 避免使用负margin，优先使用flexbox或grid

### 2.2 定位

- **position**: 优先使用`relative`和`absolute`
- **z-index**: 使用合理的z-index层级管理

## 3. 样式组织

### 3.1 导入顺序

```css
/* 1. 基础样式 */
@import 'base/xxx1.css';
@import 'base/xxx2.css';

/* 2. 工具样式 */
@import 'utils/xxx1.css';
@import 'utils/xxx2.css';

/* 3. 布局样式 */
@import 'layout/xxx1.css';
@import 'layout/xxx2.css';

/* 4. 组件样式 */
@import 'components/xxx1.css';
@import 'components/xxx2.css';

/* 5. 页面样式 */
@import 'pages/home.css';
```

## 4. 性能优化

- **避免重复**: 合并相同的CSS属性
- **使用简写**: 合理使用CSS简写属性
- **避免冗余**: 删除未使用的CSS规则

## 5. 最佳实践

### 5.1 代码组织

- **模块化**: 按功能模块组织CSS
- **可维护性**: 保持代码的可读性和可维护性
- **可复用性**: 创建可复用的CSS片段

### 5.2 变量使用

重复使用的内容提取为变量

```css
/* 使用CSS自定义属性 */
:root {
  --primary-color: #007bff;
  --secondary-color: #6c757d;
  --font-size-base: 14px;
  --spacing-unit: 8px;
}

.button {
  background-color: var(--primary-color);
  font-size: var(--font-size-base);
  padding: var(--spacing-unit) calc(var(--spacing-unit) * 2);
}
```

### 5.3 语法规范

1. 禁止使用`!important`语法，只能利用选择器优先级实现效果
