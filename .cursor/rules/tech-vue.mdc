---
description: 技术层：禁止使用 .vue 模板
globs: **/*.vue
---
# 禁止使用 .vue 文件

## 规则
- 本项目统一采用 **TSX** 编写 Vue 组件。
- **严禁创建**任何以 `.vue` 为后缀的文件。
- 如果需要创建新组件，请使用 `.tsx` 格式，并参考 `.cursor/docs/component-guide.md`。

## 处理方式
- 如果 AI 尝试生成 `.vue` 代码，必须立即停止并转换为 `.tsx` 实现。
