import { defineComponent } from "vue";

import "./index.scss";

const App = defineComponent({
  name: "App",
  setup() {

    return render;

    function render() {
      return <div class="dev-tools">frontend development tools</div>
    }
  }
});

export default App;